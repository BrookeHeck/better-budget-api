components:
  schemas:
    Account:
      type: object
      description: bank, loan, or credit account details
      required:
        - name
        - balance
        - accountType
        - userId
      properties:
        accountId:
          type: integer
          format: int64
          example: 123
          description: account identifier
        name:
          type: string
          description: Name of the account
          example: Savings Account
          nullable: false
        balance:
          type: number
          format: double
          example: 100.49
          nullable: false
        accountType:
          type: string
          example: SAVING
          description: The type of account
          nullable: false
          enum:
            - CHECKING
            - SAVING
            - LOAN
            - CREDIT
        userId:
          type: integer
          format: int64
          example: 30
          description: User id of the account owner
          nullable: false
