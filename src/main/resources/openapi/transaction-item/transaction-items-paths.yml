paths:
  /transaction-item:
    post:
      operationId: createTransactionItem
      tags:
        - transaction_item
      requestBody:
        content:
          application/json:
            schema:
              $ref: './transaction-items-components.yml#/components/schemas/TransactionItem'
      responses:
        '201':
          description: Successfully created transaction item
          content:
            application/json:
              schema:
                $ref: './transaction-items-components.yml#/components/schemas/TransactionItem'
    put:
      operationId: updateTransactionItem
      tags:
        - transaction_item
      description: Update transaction item
      requestBody:
        content:
          application/json:
            schema:
              $ref: './transaction-items-components.yml#/components/schemas/TransactionItem'
      responses:
        '200':
          description: Successfully updated transaction item
          content:
            application/json:
              schema:
                $ref: './transaction-items-components.yml#/components/schemas/TransactionItem'
  /transaction-item/{transactionId}:
    get:
      operationId: getTransactionItemsByTransactionId
      tags:
        - transaction_item
      description: Get transaction items by transaction id
      parameters:
        - name: transactionId
          in: path
          required: true
          description: Transaction id
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Successfully retrieved transaction items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: './transaction-items-components.yml#/components/schemas/TransactionItem'
  /transaction-item/{transactionItemId}:
    delete:
      operationId: deleteTransactionItem
      tags:
        - transaction_item
      description: Delete transaction item
      parameters:
        - name: transactionItemId
          in: path
          required: true
          description: Transaction item id
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: Successfully deleted transaction item
  /transaction-item/budget/{budgetCategoryId}:
    get:
      operationId: getTransactionItemsForBudgetCategory
      tags:
        - transaction_item
      description: Get transaction items for budget category id
      parameters:
        - name: budgetCategoryId
          in: path
          required: true
          description: Id of user
          schema:
            type: integer
            format: int64
        - name: date
          in: query
          schema:
            type: string
            format: date
          description: Month and year to get transaction items
          example: "2026-01-01"
          required: true
      responses:
        '200':
          description: Successfully retrieved transaction items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: './transaction-items-components.yml#/components/schemas/TransactionItem'
